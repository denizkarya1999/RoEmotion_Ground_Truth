# Install all Python dependencies for both desktop applications with:
#   python -m pip install -r requirements.txt
numpy>=1.24
sounddevice>=0.4.6
matplotlib>=3.7
pyserial>=3.5

# Tkinter is included with many Python distributions. On Debian/Ubuntu Linux,
# the GUIs and microphone support may also require these system packages:
#   sudo apt install python3-tk libportaudio2 portaudio19-dev
